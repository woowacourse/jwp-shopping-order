spring:
  sql:
    init:
      data-locations:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL
    driverClassName: org.h2.Driver
    username: sa
    password:
  test:
    database:
      replace: none
