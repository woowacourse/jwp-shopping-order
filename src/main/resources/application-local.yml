spring:
  config:
    activate:
      on-profile: local
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/shopping_order?characterEncoding=UTF-8&serverTimezone=UTC
    username: root
    password: root
  sql:
    init:
      mode: always
