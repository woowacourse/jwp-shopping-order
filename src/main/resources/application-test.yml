spring:
  sql:
    init:
      mode: always
  config:
    activate:
      on-profile: test
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;MODE=MySQL
  h2:
    console:
      enabled: true
