spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/shopping?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: root123

server:
  port: 80
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
