spring:
  thymeleaf:
    cache: false
  h2:
    console:
      path: /h2-console
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
  profiles:
    active: ${profile}
  sql:
    init:
      data-locations: classpath:resources/schema.sql
      mode: always
